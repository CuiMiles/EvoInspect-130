# 初赛提交物工作区

`drafts/` 中的 PDF、MP4 和 ZIP 是可审查草稿。正式上传前必须替换以下占位信息：团队名称、
参赛组别、作者/成员、学校及分工，并按官方规则重命名。任何 GTX 2060 性能数字只能在真实
设备运行 `scripts/benchmark_efficientad_latency.py` 后写入。

机器约束验收记录写入 `evidence/submission_artifact_validation.json`；生成源文件保留在本目录，
避免只保存不可修改的二进制成品。
