# AGENTS.md - EvoInspect-130 长期研发规则

## 1. 任务

构建 **EvoInspect-130**：一套面向工业组装场景的图像/视频 AOI 系统，在仅获得 100 张正常样本和 30 张缺陷样本后快速迁移，识别外观、结构、颜色、缺件、尺寸偏差、逻辑和顺序异常，并通过操作员反馈进行安全、自学习、可回滚的持续优化。

目标是形成国赛级研究作品。不得声称奖项有保证。

## 2. 每次工作前必须读取

依次读取：

1. 本文件；
2. `project_spec.yaml`；
3. `STATUS.md`；
4. `docs/01_OFFICIAL_AOI_SPEC.md`；
5. 当前任务涉及的其他 `docs/*.md`；
6. `evidence/claim_ledger.csv`；
7. 最近一次实验报告与未解决问题。

若官方附件与仓库文档冲突，以最新官方附件和组织方书面答复为准。

## 3. 真实性与创新边界

- 绝不伪造指标、日志、截图、模型结果、用户反馈、硬件延迟、论文结论或代码完成状态。
- 绝不使用测试标签调阈值、挑模型、早停或生成训练样本。
- 绝不把上游代码改名后声称原创。
- 允许并鼓励增量创新、组合创新、任务特定创新和系统创新；但每项声明必须写明最近相关方法与精确差异。
- 未完成系统检索和对照实验前，不使用“首次”“首创”“国际领先”“全面超越”等表述。
- 可使用“我们提出”“面向该评测协议设计”“在统一框架中实现”“实验表明在所测协议上”等可验证表述。
- 每项保留的创新必须有受控消融。没有稳定收益或工业价值的模块应删除或降级为探索项。

## 4. 数据与评测纪律

- 原始数据只读；派生数据放在 `data/derived/` 或外部缓存。
- 所有划分使用内容哈希去重，记录数据来源、许可证、类别、缺陷类型和划分哈希。
- 至少保留：类别外推、缺陷类型外推、环境漂移、时间顺序和公开基准标准协议。
- 100 正常 + 30 缺陷的适配支持集与最终测试集严格隔离。
- 公开测试、内部开发、最终隐藏模拟三套阈值和配置不得交叉泄漏。
- 每个实验记录：Git commit、dirty 状态、配置哈希、数据/划分哈希、seed、硬件、环境、起止时间、输出指标、模型哈希和失败原因。
- 至少 3 个随机种子用于日常判断；最终主表尽量使用 5 个种子或给出 bootstrap 置信区间。

## 5. 性能基准纪律

- 官方风格时延默认 batch size = 1。
- 分别测量预处理、模型推理、后处理、序列化和端到端时延。
- CUDA 测量前预热并同步；报告 p50、p95、最大值和吞吐量，不只报告最好一次。
- 记录输入分辨率、精度、TensorRT/ONNX/PyTorch 版本、驱动、CUDA、GPU、功耗/频率设置。
- 未在 GTX 2060 或等价更低设备实测，不得写成已满足官方 200ms；只能写“3090 实测”或“模型估算”。
- CPU <2s 是挑战目标，未实测不得宣称。

## 6. 在线系统边界

最终在线推理图：

- 不使用远程 API；
- 不依赖大语言模型；
- 不运行大扩散模型；
- 神经网络主体不超过三个；
- 支持离线部署、版本化、回滚和确定性配置；
- 输出异常分数、区域/掩码、缺陷类别或未知类型、置信度、时延分解和模型版本；
- 视频输出事件级时间段和顺序/逻辑解释。

大型视觉基础模型、扩散模型和多模型集成只能作为离线教师、数据生成或研究基线，最终通过蒸馏、剪枝、量化或替代模块退出在线图。

## 7. 自学习安全边界

操作员反馈不能直接无门槛修改线上主模型。

必须采用两级更新：

1. 可逆即时更新：阈值、校准、原型/记忆库、规则或黑白名单；
2. 候选模型更新：带回放、蒸馏/正则、独立验证、影子测试和回滚。

候选版本只有在反馈切片改进且锚定回归集不超过允许退化时才能发布。保存版本、变更原因、训练数据哈希和回滚点。

## 8. 代码与仓库质量

- Python 使用 `pyproject.toml`、类型标注、ruff、mypy、pytest。
- 核心逻辑写成可测试模块，不以 notebook 为唯一实现。
- 配置写入 YAML/TOML；不得把关键阈值散落硬编码。
- 所有命令可重复运行；失败返回非零退出码。
- 使用结构化日志，包含 run_id、commit、config_hash、seed、device 和 model_hash。
- 不提交密钥、原始大型数据、缓存、私人链接或未经许可资产。
- 引入上游项目时记录 commit、许可证、修改文件和复现命令。

## 9. GPU 调度

优先把 8 张 3090 用于独立实验并行，而不是为小模型无意义地做 DDP。

- GPU 0：轻量基线与最终学生模型；
- GPU 1：PatchCore/AnomalyDINO/记忆方法；
- GPU 2：AHL/DRA/开放集监督路线；
- GPU 3-4：缺陷合成与教师模型；
- GPU 5：高分辨率路由与压缩；
- GPU 6：逻辑/视频和持续学习；
- GPU 7：独立复验、性能基准或备用。

每个长任务必须有预算、早停、检查点和显存上限。不得让一个不确定实验长期占满全部 GPU。

## 10. 持续工作方式

一个 Codex 会话不可能完成数月研发。每次会话必须：

1. 从 `STATUS.md` 恢复；
2. 选择预期价值最高且当前可执行的工作包；
3. 实现、测试、运行或排队实验；
4. 分析证据；
5. 更新 `STATUS.md`、实验登记和 claim ledger；
6. 留下恰好一个主推荐动作和可并行任务列表。

遇到阻塞时先处理其他不依赖任务，不因一个实验失败停止整个项目。

## 11. 每次结束格式

必须输出：

1. `Status: PASS | PARTIAL | FAIL`
2. 本轮结论；
3. 变更文件；
4. 执行命令；
5. 测试和真实指标；
6. 证据路径；
7. 风险/阻塞；
8. 下一主动作；
9. 可并行实验。
